export default class Commands {

    static parseString(message) {
        let parts = message.toLowerCase().split(' ');
        switch (parts[0]) {
            case 'scan':
                
        }
    }

}